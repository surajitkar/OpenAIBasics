name: My First workflow

on: [push]


jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout code
          uses: actions/checkout@v4

        - name: List and read files
          run: |
            echo "Hello, World! This is my first GitHub Actions workflow."
            ls -ltra
            cat README.md

        - name: Generate ASCII Artwork
          run: cowsay -f dragon "Hello, World! This is my first GitHub Actions workflow."
        

        # - name: List files
        #   run: ls -la

        # - name: Read file
        #   run: cat README.md